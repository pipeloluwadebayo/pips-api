apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pips-api.fullname" . }}-test-connection"
  labels:
    {{- include "pips-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pips-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
